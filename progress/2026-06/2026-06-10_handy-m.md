---
project_slug: handy-m
date: 2026-06-10
updated: 2026-06-10
updated_by: codex
---

# 2026-06-10 Progress Log: handy-m

## 概要

- Handy_m を upstream Handy 最新へ追従し、Mac側でビルド検証した。Windows利用に向けてGitHub pushとWindows Codex引き継ぎを進める。

## 完了した作業

- `main` を upstream `origin/main` の `7901ef7` へfast-forward。
- `shotaro/custom` に `main` をマージし、`v0.8.3` 後のupstream追加コミットを取り込み。
- `bun install` で依存関係を同期。lockfile差分なし。
- Prettier指摘のMarkdown/YAMLを整形。
- Mac側でfrontend/Rust/Tauri buildを検証。
- `progress/` を初期化し、Mac/Windows Codex共有の現在地を作成。
- Windowsローカルビルド阻害要因になる upstream のAzure Trusted Signing用 `signCommand` を削除。

## 変更ファイル

- `AGENTS.md`
- `README.md`
- `src-tauri/tauri.conf.json`
- `.serena/memories/*.md`
- `.serena/project.yml`
- `progress/progress.md`
- `progress/2026-06/2026-06-10_handy-m.md`
- `progress/cleanup-candidates.md`

## 検証

- `bun install`: 成功。
- `bun run format:check`: 成功。
- `bun run lint`: 成功。
- `bun run build`: 成功。Vite chunk size warningのみ。
- `bun run check:translations`: 成功。19言語すべてkey完全。
- `RUSTC=... cargo check`: 成功。`rustc 1.94.1` を明示。
- `bun run tauri build`: 成功。`Handy_m.app` と `Handy_m_0.8.3_aarch64.dmg` を生成。

## 決定事項

- 個人カスタムは `shotaro/custom` に保持する。
- upstream release最新は `v0.8.3` だが、upstream `main` の追加10コミットも取り込んだ状態をWindowsへ渡す。
- MacのTauri buildでは `/usr/bin/xattr` を優先するPATHが必要。
- 個人forkのWindows installerは未署名でビルドする。署名が必要な配布時は別途方針を決める。

## Blocker / Risk

- Windows固有のbuild、起動、音声入力、IME/貼り付け挙動、DirectML/Vulkan利用はWindows側で未検証。
- MacのHomebrew `rustc 1.87.0` では依存要件不足。`rustup` stable `rustc 1.94.1` で通した。

## 引き継ぎ / 次

- 個人GitHub forkを作成し、`shotaro/custom` をpushする。
- Windows Codexに shared folder へのcloneとWindows最適化・実機検証を依頼する。

## 参照ログ

- なし。

## 削除候補

- なし。
