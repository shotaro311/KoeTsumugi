---
project_slug: handy-m
date: 2026-08-09
status: active
branch: codex/koetsumugi-1.0.3-acceptance
base_head: 17d3269
---

# KoeTsumugi 1.0.3 Windows NSIS最終受入

## 目的

停止した前worktree `0573` の未commitブランド移行を現worktree `6019` へ安全に再現し、現sourceとローカル実ファイルを正本としてWindows NSISのupload可否を判定する。push、release、公開、既存アプリへのinstall、削除、署名鍵変更は行わない。

## 移行

- 現worktreeがupstream `8a362e9`のdetached HEADだったため、既存1.0.2との互換性を優先し、`shotaro/custom`の基盤`17d3269`から`codex/koetsumugi-1.0.3-acceptance`を作成した。
- 前worktreeのtracked差分とuntracked成果物を再現。変更対象166ファイルのSHA-256、削除一覧、Git状態件数を照合して一致した。
- license inventoryを現時刻で再生成したため、生成時刻だけが前worktreeから更新された。依存内容はWindows x64到達542 package、本文285種類、package-local本文なし56件で不変。

## 検証

- `git diff --check`、`bun run format:check`、`bun run lint`、`bun run check:translations`、`bun run build`: 成功。Viteの既存chunk size warningのみ。
- `cargo test --locked`: 142件成功、失敗0。
- `cargo clippy --locked --lib --tests`: 成功。既存warningのみ。
- release `koetsumugi.exe --help`と`--list-devices`: exit 0。KoeTsumugi表示、Vulkan 3 deviceとCPUを確認。
- 既存updater鍵を値非表示で使用し、NSISと`.sig`を生成。repository外の`minisign-verify`ベース検証ツールで、sourceに埋め込まれた公開鍵に対する署名を検証した。
- 最終 `KoeTsumugi_1.0.3_x64-setup.exe`: 21,171,481 bytes、SHA-256 `FE9ABE4F5531FDD526E75789DFC2EDFD8C2F7EA75A1B294C33B4868620423B42`、ProductName/FileDescription `KoeTsumugi`、version `1.0.3`。Authenticodeは未署名。
- 生成NSIS scriptに旧`handy.exe`検出、旧registry/shortcut/autostart移行、法的資料5件の同梱を確認した。
- identifier、updater公開鍵、endpointはbase HEADと一致し、`com.shotaro.handym`の設定・履歴・モデル保存先を維持する。

## 実更新のreadback

- 今回はinstallを実行せず、2026-08-08の更新前退避と現在のインストール状態を読み取った。
- 現在は旧install場所の`koetsumugi.exe` 1.0.3が起動中。旧`handy.exe`、旧uninstall登録、旧Start Menu shortcutはなく、KoeTsumugiのuninstall登録、shortcut target、autostart targetが正しい。
- 更新前退避と現在でRoaming 18ファイルの欠落・追加なし。`history.db`、録音15件はhash一致。`settings_store.json`は更新後に変更されていたが、変更leafは`settings.whats_new_last_seen_version`だけで、設定schemaと他フィールドは保持されている。
- model 2件は双方に存在。1件はhash一致し、`gigaam_vocab.txt`だけが出所固定版へ意図的に更新されたという前回記録と整合する。

## 最終判定

- **Windows NSIS: upload可。** 現sourceから最終署名build、独立updater署名検証、移行script・法的資料同梱、前回1.0.2実更新の退避と現在状態のreadbackまで確認した。
- **Windows MSI: upload不可。** 旧Handy_m移行処理がなく、release workflowもNSIS限定。
- **macOS: upload不可。** `tauri-nspanel 2.1.0`のlicense不明が未解決。
- 残る注意点はfull uninstall/rollback未試験、Windows Authenticode未署名、正式商標クリアランス未実施、package-local本文なし56件のreview。これらはWindows NSISの技術的uploadを止めないが、公開時に明示する。

## 外部状態

- commit、push、release、公開、既存アプリへのinstall、削除、署名鍵変更は実施していない。
