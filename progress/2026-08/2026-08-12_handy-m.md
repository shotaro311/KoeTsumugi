# 2026-08-12 Mac導入と正式配布対応

## 目的

- このMacへ現在の公開最新版KoeTsumugiを導入する。
- インストール済みの旧Handy_mをアンインストールし、既存の設定・履歴・録音・モデルは保持する。

## 実施

- GitHub APIで公開最新版が`KoeTsumugi v1.0.4`、tag `handy-m-v1.0.4`、release commit `d6eeacd0c454ab56c686fe2a8d7b5c3f63d2bd45`であることを確認した。
- 公開releaseはWindows x64 NSISのみでmacOS binaryを提供していないため、公式repositoryの`shotaro/custom`をcommit `a2fc387ade11889cf331baddef15459f9483d826`へfast-forwardした。release tag以降は公開結果とローカルbranch整理のdocs commitだけ。
- `bun install --frozen-lockfile`、format、lint、frontend build、23言語のtranslation checkを実行した。
- Rust stable 1.94.1とBun 1.3.10で`bun run tauri build --bundles app`を実行し、Apple Silicon向け`KoeTsumugi.app`を生成した。
- 実行中の旧アプリがないことを確認して`/Applications/Handy_m.app`を`~/.Trash/Handy_m.app`へ移動した。`~/Library/Application Support/com.shotaro.handym`は削除していない。
- 生成したapp bundleを`/Applications/KoeTsumugi.app`へinstallし、`--start-hidden`で起動した。

## Readback

- install先: `/Applications/KoeTsumugi.app`
- 表示名: `KoeTsumugi`
- version: `1.0.4`
- bundle ID: `com.shotaro.handym`
- architecture: `arm64`
- build/install binary SHA-256: `00e3c34140d4b4fb2003165398bcf5338ecf2dc0ae19ccbdfae7d41b33e95b1e`
- `codesign --verify --deep --strict`: 成功。ローカルbuildのad-hoc署名で、notarizationは実施していない。
- 旧`/Applications/Handy_m.app`: 不在。ゴミ箱の`Handy_m.app`を確認し、復元可能。
- app data: 28ファイル、約475MB。model file合計491,852,915 bytesを保持。
- runtime: KoeTsumugi processを確認。既存`history.db`はmigration前version 4で最新、マイク`BOYA mini`初期化、最初のaudio chunk、Metal/CPU compute device初期化、SMAppServiceログイン項目登録をログで確認した。
- Git: `shotaro/custom`と`origin/shotaro/custom`はahead/behind `0/0`。進捗記録前のworktreeはcleanだった。

## 注意点

- macOS公式配布はなく、今回のinstallは公開sourceからのローカルbuild。`tauri-nspanel 2.1.0`の再配布許諾が未確定のため、macOS binaryの公開可否とは分けて扱う。
- 実音声による新規文字起こしと貼り付けは、この導入作業では実施していない。起動、マイク入力stream、既存データ、認識backendの初期化までを確認した。

## KoeTsumugi 1.0.5 正式配布対応

- versionを1.0.5へ同期し、Apple Silicon Mac向けrelease notes、README、配布手順、release workflowを更新した。
- `tauri-nspanel 2.1.0`のlocked commitに`LICENSE_MIT`と`LICENSE_APACHE-2.0`が存在することを確認し、license overrideとsource commitを台帳へ固定した。
- Windows x64とmacOS arm64を統合した依存台帳を再生成した。633 package、manual review 0件、package-local本文なし85件、収録license/notice本文308種。
- release workflowはWindows x64完了後にApple Silicon版をDeveloper ID署名し、appとDMGを別々にApple公証・stapleしてからdraft releaseへuploadする。必要assetが揃わない場合は公開しない。
- GitHub ActionsへDeveloper ID証明書、証明書password、一時keychain password、Apple ID、Team IDを暗号化Secretとして登録した。値はログと進捗へ記録していない。

## 1.0.5 公開前Readback

- `bun install --frozen-lockfile`、format、lint、frontend build、23言語447 key、Rust test 202件、通常Clippy、GitHub Actions workflow構文検査が成功した。Clippyは公式コード由来の既存warning 4件のみ。
- Developer ID Application `N7VVPW44ZA`でarm64 appを署名し、Apple公証submission `324d9084-eb0d-495a-a375-faa3a04b2373`は`Accepted`。appへのstaple、`spctl`の`Notarized Developer ID`判定、version 1.0.5、bundle ID `com.shotaro.handym`を確認した。
- signed/notarized appを`--start-hidden`で起動し、既存history DB v4、BOYA mini入力stream、Metal library初期化を確認した。
- 公開前の残作業はApple app-specific passwordのGitHub Secret登録、commit/push、release workflow、公開assetとupdater metadataのreadback、公開DMGからの再導入、Note記事のMac対応追記。
