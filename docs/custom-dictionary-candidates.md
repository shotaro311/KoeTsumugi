# KoeTsumugi カスタム辞書 登録候補（第1回）

## チェック方法

- **登録しない候補だけ**、行頭を `[x]` にしてください。
- `[ ]` のまま残した候補は、次の作業でカスタム辞書へ登録します。
- `ChatGPT` はすでに登録済みなので、この一覧には含めていません。
- 「モデル＋後処理」は、認識時のヒントと、認識後の表記修正の両方に使う想定です。

## AI・開発ツール

- [ ] **Codex** ← `コーデックス`、`コデックス`（モデル＋後処理）
- [ ] **CodexPeer** ← `コーデックスピア`、`Codexピア`、`Codex Peer`（モデル＋後処理）
- [ ] **Claude Code** ← `クロードコード`、`Claudeコード`（モデル＋後処理）
- [ ] **OpenAI** ← `オープンAI`、`オープンエーアイ`、`Open AI`（モデル＋後処理）
- [ ] **MCP** ← `エムシーピー`、`M C P`（モデル＋後処理）
- [ ] **Obsidian** ← `オブシディアン`、`オブジディアン`（モデル＋後処理）
- [ ] **GitHub** ← `ギットハブ`、`ギッハブ`、`Git Hub`（モデル＋後処理）
- [ ] **GitHub Actions** ← `ギットハブアクションズ`、`GitHubアクションズ`、`GitHub Actions`（モデル＋後処理）
- [ ] **LM Studio** ← `エルエムスタジオ`、`LMスタジオ`、`L M Studio`（モデル＋後処理）
- [ ] **Hugging Face** ← `ハギングフェイス`、`ハグングフェイス`、`HuggingFace`（モデル＋後処理）

## 最近扱っているアプリ・プロジェクト

- [ ] **KoeTsumugi** ← `こえつむぎ`、`コエツムギ`、`Koe Tsumugi`（モデル＋後処理）
- [ ] **HoverPocket** ← `ホバーポケット`、`ホバーポケットアプリ`、`Hover Pocket`（モデル＋後処理）
- [ ] **ComfyRemote** ← `コンフィリモート`、`コンフィーリモート`、`Comfy Remote`（モデル＋後処理）
- [ ] **ComfyUI** ← `コンフィユーアイ`、`コンフィUI`、`Comfy UI`（モデル＋後処理）
- [ ] **Hermes Agent** ← `ヘルメスエージェント`、`ハーミーズエージェント`、`Hermesエージェント`（モデル＋後処理）
- [ ] **amalife** ← `アマライフ`、`AmaLife`（モデル＋後処理）
- [ ] **Codex Image Canvas** ← `コーデックスイメージキャンバス`、`Codex画像キャンバス`、`Codex ImageCanvas`（モデル＋後処理）
- [ ] **Anima-Turbo** ← `アニマターボ`、`Anima Turbo`（モデル＋後処理）

## 音声認識モデル

- [ ] **Cohere Transcribe** ← `コヒアトランスクライブ`、`コヒーアトランスクライブ`、`Cohere文字起こし`（モデル＋後処理）
- [ ] **Whisper** ← `ウィスパー`、`ウイスパー`（モデル＋後処理）
- [ ] **Nemotron** ← `ネモトロン`、`Nemtron`（モデル＋後処理）
- [ ] **Parakeet** ← `パラキート`、`パラキートTDT`（モデル＋後処理）
- [ ] **Voxtral** ← `ヴォクストラル`、`ボクストラル`（モデル＋後処理）
- [ ] **Canary** ← `カナリー`、`Canaryモデル`（モデル＋後処理）

## 最近使っているモデル名・配布関連

- [ ] **GPT-5.6 Sol** ← `GPT 5.6 Sol`、`GPT5.6 Sol`、`5.6 Sol`、`GPTごてんろくソル`（モデル＋後処理）
- [ ] **GPT-5.6 Terra** ← `GPT 5.6 Terra`、`GPT5.6 Terra`、`5.6 Terra`、`GPTごてんろくテラ`（モデル＋後処理）
- [ ] **GPT-5.6 Luna** ← `GPT 5.6 Luna`、`GPT5.6 Luna`、`5.6 Luna`、`GPTごてんろくルナ`（モデル＋後処理）
- [ ] **App Store Connect** ← `アップストアコネクト`、`AppStore Connect`、`App Storeコネクト`（モデル＋後処理）
- [ ] **TestFlight** ← `テストフライト`、`Test Flight`（モデル＋後処理）

## 今回あえて入れていない語

`Rust`→「ラスト」、`Bun`→「バン」、`LoRA`→「ローラ」のように、読みが一般語や人名と重なるものは、誤置換を避けるため第一弾から外しています。実際の文字起こし履歴で誤認識が続く場合は、後処理を使わず「モデル用ヒントのみ」で追加します。
