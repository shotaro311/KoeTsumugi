## マルチGPU環境の安定性を改善

GPU番号を指定して起動したとき、通常のデスクトップ版でも指定先へ音声認識モデルを
読み込むようにしました。複数のNVIDIA GPUを切り替えて使う環境で、外部のGPU追従設定と
実際のモデル配置が食い違いにくくなります。

## 音声入力中の意図しない再起動を防止

表示GPUへ追従させる外部連携から起動された場合も、指定されたGPUをアプリ全体で一貫して
使用します。GPU不一致と誤判定されてアプリや音声入力が突然閉じる問題を改善しました。
